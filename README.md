# xcpc\_template\_cpp

xcpc模板,c++

### 1.树状数组

另外还支持O(logn)实现下面两个功能

(1)前缀和 <= k 的最大的下标

(2)查找前缀和 >= k 的最小下标

如果需要查找第k大的元素，树中存的数据即为标记。数据范围过大时需要离散化，且添加或者删除的元素需要在创建树前已知。


### 2.笛卡尔树


### 3.最近公共祖先


### 4.阶乘，阶乘逆元，快速幂，组合数


### 5.Dijkstra


### 6.Bellman_Ford 判断负环，0是超级源点，其他点位 1-n


### 7.Bellman_Ford的queue优化 spfa 


### 8.Mex 函数


### 9.欧拉筛


### 10.并查集


### 11.nim游戏
#### 1.阶梯nim
偶数层无效，只需查看奇数层nim和

#### 2.反nim游戏(最后一个拿的玩家失败)
奇异局势Y：num-sum = 0; 非奇异局势N：num-sum0 != 0
定义：第i堆为充裕堆：a[i] > 1；第i堆为孤单堆：a[i] = 1，充裕堆的数量cnt
NO, NI, N2：非奇异局势下，cnt 分别为0,1, >=2
Y0, Y1, Y2：奇异局势下，cnt 分别为0,1, >=2
显然：N能转移到Y,也能转移到N；只能转移到N
1、N0必败，Y0必胜
2、N1必胜，N1可以转移到N0
3、N2必胜，Y2必败
Y2只能转移到N1和N2：若Y2转移到N1（必胜态），则Y2败；若Y2转移到N2，
N2只需要转回到Y2即可,Y1不可能达到），直到Y2只能转移到N1为止,所以Y2必败，N2必胜。
必胜态：Y0, N1, N2
必败态：NO, Y2

### 12.线段树
简化参数成员函数为 1_base 起始下标
可手动变为 0_base 起始下标

### 13.mod性质
r = x % p, x = k * p + r, r <= x / 2
对 x 取模 p 结果 r, r 最多为 x 的一半